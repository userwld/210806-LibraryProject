package memberInfo.service;

import javafx.scene.Parent;

public interface Memberinfo {

	void lookupProc(Parent memInfoForm);

	void rentalProc(Parent memInfoForm);

	void returnProc(Parent memInfoForm);

}
