package preview.service;

import javafx.scene.Parent;

public interface PreviewService {

	void likeClick(Parent newPreViewForm, String likeName);
	void likeClick1(Parent newPreViewForm, String likeName);
	void openarea(Parent newPreViewForm);
	void closearea(Parent newPreViewForm);
}
